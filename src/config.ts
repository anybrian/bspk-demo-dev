/**
 * A centeralized source for all the constants hidden and referenced in various files.
 *
 * Each item should be explained in detail, including its purpose and usage.
 */

/**
 * Property tables are sorted in various ways and eventually sorted alphabetically, these are property names that should
 * be in this specified order.
 */
export const PROPERTY_NAME_CUSTOM_SORT = [
    //
    ['minRows', 'maxRows'],
    ['min', 'max'],
    ['header', 'body'],
    ['variant', 'width', 'height'],
];
